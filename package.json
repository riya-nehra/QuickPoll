{
  "name": "quickpoll",
  "version": "1.0.0",
  "description": "A lightweight real-time polling app built with Firebase and Vercel",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
